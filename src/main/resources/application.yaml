spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: test
    password:
  sql:
    init:
      mode: always

mybatis:
  mapper-locations: classpath:mapper/*.xml